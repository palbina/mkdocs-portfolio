# Tasks: Portfolio Improvements

**Input**: Design documents from `/specs/007-portfolio-improvements/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, quickstart.md

**Tests**: Not requested in spec. Manual verification via `zensical build` and browser inspection.

**Organization**: Tasks grouped by user story for independent implementation.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3, US4)
- Include exact file paths in descriptions

## Path Conventions

- Documentation content: `docs/`, `docs/blog/posts/`, `docs/stylesheets/`
- Configuration: `mkdocs.yml`, `.gitignore`, `CONTRIBUTING.md`
- Root artifacts: `=0.0.24`

---

## Phase 1: Setup (Verify Baseline)

**Purpose**: Ensure the build toolchain works before making changes.

- [X] T001 Verify `zensical build --clean` succeeds on current branch with zero errors
- [X] T002 Verify existing 6 project cards render correctly in `docs/index.md` grid

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Investigate any unknowns before implementing changes. No code changes yet.

- [X] T003 [P] Investigate whether `=0.0.24` residue file is regenerated by `zensical build` (run build, check if file reappears) — determines FR-006 strategy (delete vs gitignore)
- [X] T004 [P] Verify GitHub Actions workflow `.github/workflows/deploy.yml` is vestigial (check if any deployment target references it; if uncertain, leave in place and document in CONTRIBUTING.md only)

**Checkpoint**: Investigation complete. Strategy for residue file confirmed. CI cleanup decision made.

---

## Phase 3: User Story 1 - Blog Content Completion (Priority: P1) 🎯 MVP

**Goal**: Blog section has 3 complete posts with zero placeholder text. The Talos Linux post's section 3 is finished.

**Independent Test**: `ls docs/blog/posts/*.md | wc -l` ≥ 3. `grep -c "Aquí relataré" docs/blog/posts/2026-02-10-talos-linux-k8s.md` = 0.

### Implementation for User Story 1

- [X] T005 [US1] Complete section 3 ("Mi Experiencia de Migración") in `docs/blog/posts/2026-02-10-talos-linux-k8s.md` — replace placeholder `(Aquí relataré brevemente...)` with real migration experience: boot time comparison (2min→20s), migration steps, challenges encountered, and a metrics table matching the template format.
- [X] T006 [P] [US1] Create blog post `docs/blog/posts/2026-07-05-gitops-real-world.md` following `TEMPLATE-BLOG-POST.md` structure — topic: ArgoCD ApplicationSets and sync waves from the GitOps project. Include Mermaid diagram, implementation steps with code blocks, production metrics table, and "Cuándo Usar (y Cuándo No)" tabs.
- [X] T007 [P] [US1] Create blog post `docs/blog/posts/2026-07-05-observability-correlation.md` following `TEMPLATE-BLOG-POST.md` structure — topic: LGTM signal correlation (Loki+Tempo+Prometheus) from the observability project. Include debugging scenario walkthrough, PromQL examples, metrics table, and "Cuándo Usar (y Cuándo No)" tabs.
- [X] T008 [US1] Verify blog build: run `zensical build --clean` and confirm 3 posts appear in `site/blog/index.html` with correct excerpts, author info, category links, and zero placeholder text.

**Checkpoint**: Blog index shows 3 complete posts. Talos Linux post has real migration content.

---

## Phase 4: User Story 2 - Landing Page Consistency (Priority: P1)

**Goal**: Landing page displays all 8 project cards. Dead `.hero-stats` CSS removed.

**Independent Test**: `grep -c "project-card" docs/index.md` ≥ 8. `grep "hero-stats" docs/stylesheets/extra.css` returns zero lines.

### Implementation for User Story 2

- [X] T009 [US2] Add Progressive Delivery project card to `docs/index.md` `.projects-grid` div — use 🚀 icon, "Progressive Delivery" title, "ZERO-DOWNTIME DEPLOYS" impact metric, tech tags (Argo Rollouts, Istio, Prometheus), and link to `projects/progressive-delivery/`.
- [X] T010 [US2] Add Database HA project card to `docs/index.md` `.projects-grid` div — use 🗄️ icon, "Database HA (CNPG)" title, "POSTGRES REPLICATION" impact metric, tech tags (Postgres, CNPG, Barman), and link to `projects/database-ha/`.
- [X] T011 [US2] Remove dead `.hero-stats`, `.stat`, `.stat-number`, `.stat-label` CSS rules from `docs/stylesheets/extra.css` — delete the unused blocks (main, light mode override, mobile override sections).
- [X] T012 [US2] Verify landing page build: run `zensical build --clean` and confirm 8 project cards in grid, no dead CSS selectors for hero-stats, all card links resolve correctly.

**Checkpoint**: Landing page shows 8 project cards. Extra.css has no unused hero-stats CSS.

---

## Phase 5: User Story 3 - Maintenance & Tooling Cleanup (Priority: P1)

**Goal**: CONTRIBUTING.md references Zensical commands. Residue file cleaned up.

**Independent Test**: `grep "zensical build" CONTRIBUTING.md` returns ≥1 match. `test -f =0.0.24` returns false (or file is gitignored).

### Implementation for User Story 3

- [X] T013 [US3] Update local development section in `CONTRIBUTING.md` — replace `mkdocs build --strict` and `mkdocs serve` with `zensical build --clean` and `zensical serve`. Add note that `mkdocs.yml` is retained as historical reference and `zensical.toml` is the active config. Reference `docs/MIGRATION-ZENSICAL.md` for migration details.
- [X] T014 [US3] Handle residue file `=0.0.24` per T003 investigation result: file does NOT regenerate → deleted permanently with `rm =0.0.24`.
- [X] T015 [US3] Verify tooling cleanup: run `zensical build --clean`, confirm CONTRIBUTING.md has no `mkdocs build` as primary command, confirm residue file deleted.

---

## Phase 6: User Story 4 - SSG Configuration Simplification (Priority: P2)

**Goal**: mkdocs.yml clearly marked as historical reference. Broken macros plugin reference removed.

**Independent Test**: `head -5 mkdocs.yml` shows historical comment. `grep "scripts/main" mkdocs.yml` returns zero matches.

### Implementation for User Story 4

- [X] T016 [US4] Add header comment to `mkdocs.yml` — insert before line 1 the historical reference notice stating zensical.toml is canonical and builds use zensical exclusively. Reference `docs/MIGRATION-ZENSICAL.md`.
- [X] T017 [US4] Remove the broken `macros` plugin block from `mkdocs.yml` — delete the lines `- macros:` and `module_name: scripts/main` since `scripts/main.py` was deleted in March 2026.
- [X] T018 [US4] Verify config cleanup: confirm `head -10 mkdocs.yml` shows historical reference comment, confirm `grep "scripts/main" mkdocs.yml` = 0, confirm `zensical build --clean` succeeds with zero issues.

**Checkpoint**: mkdocs.yml has historical header and no broken plugin references. Build succeeds.

---

## Phase 7: Polish & Final Verification

**Purpose**: Cross-cutting validation across all user stories.

- [X] T019 Run full `quickstart.md` validation: execute all 5 verification blocks (build, blog content, landing page, CONTRIBUTING.md, residue file, mkdocs.yml cleanup).
- [X] T020 Run `zensical build --clean` final build and verify zero errors, zero warnings.
- [X] T021 Verify all 6 success criteria from spec.md pass: SC-001 (3 posts ✅), SC-002 (8 cards ✅), SC-003 (no dead CSS ✅), SC-004 (CONTRIBUTING accurate ✅), SC-005 (no residue ✅), SC-006 (no broken macros ✅).

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — verify baseline immediately.
- **Foundational (Phase 2)**: Depends on Setup. T003 and T004 run in parallel.
- **User Stories (Phase 3-6)**: All 3 P1 stories (US1, US2, US3) can start in parallel after Foundational. US4 (P2) starts after its dependent P1s or independently (US4 has no dependency on US1-US3).
- **Polish (Phase 7)**: Depends on all user stories being complete.

### User Story Dependencies

- **User Story 1 (P1)**: No dependencies on other stories. Can start immediately after Phase 2.
- **User Story 2 (P1)**: No dependencies on other stories. Can start immediately after Phase 2.
- **User Story 3 (P1)**: T014 depends on T003 (residue file investigation). Otherwise no dependencies.
- **User Story 4 (P2)**: No dependencies on other stories. Can start independently.

### Within Each User Story

- Blog posts (T006, T007) are [P] — different files, can be written in parallel
- Project cards (T009, T010) are [P] — different cards in same file can be added independently (though same file means sequential edit)
- All stories have verification tasks that run after implementation

### Parallel Opportunities

```bash
# After Phase 2 completion, launch all P1 stories in parallel:
# Developer A: User Story 1 (Blog Content)
Task T005: "Complete section 3 in docs/blog/posts/2026-02-10-talos-linux-k8s.md"
Task T006: "Create blog post docs/blog/posts/2026-07-05-gitops-real-world.md"
Task T007: "Create blog post docs/blog/posts/2026-07-05-observability-correlation.md"

# Developer B: User Story 2 (Landing Page)
Task T009: "Add Progressive Delivery card to docs/index.md"
Task T010: "Add Database HA card to docs/index.md"
Task T011: "Remove hero-stats CSS from docs/stylesheets/extra.css"

# Developer C: User Story 3 + 4 (Tooling + Config)
Task T013: "Update CONTRIBUTING.md"
Task T014: "Handle residue file =0.0.24"
Task T016: "Add historical comment to mkdocs.yml"
Task T017: "Remove macros plugin from mkdocs.yml"
```

---

## Implementation Strategy

### MVP First (User Stories 1, 2, 3 — All P1)

1. Complete Phase 1: Setup (verify baseline)
2. Complete Phase 2: Foundational (investigate unknowns)
3. Complete Phases 3, 4, 5 in parallel: Blog + Landing + Tooling
4. **STOP and VALIDATE**: Run `zensical build --clean`, verify quickstart.md checks
5. If all P1 stories pass, deploy/demo

### Incremental Delivery

1. Setup + Foundational → Baseline confirmed, investigation done
2. Add US1 (Blog) → 3 complete posts, test independently
3. Add US2 (Landing) → 8 cards, no dead CSS, test independently
4. Add US3 (Tooling) → accurate CONTRIBUTING.md, no residue, test independently
5. Add US4 (Config) → mkdocs.yml cleaned, test independently
6. Each story adds value without breaking previous stories

### Single Developer Strategy

Since all tasks are content/config changes in separate files or separate areas of the same file:

1. Phase 1-2: Sequential (T001→T004)
2. Phase 3 (US1): T005 first (fixes existing), then T006+T007 parallel (new posts)
3. Phase 4 (US2): T009+T010 sequential (same file), T011 independent
4. Phase 5 (US3): T013 then T014
5. Phase 6 (US4): T016 then T017
6. Phase 7: T019→T021 sequential validation

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- Each user story independently testable via its verification task
- All changes are content/config edits — no build system changes, no new dependencies
- Verify `zensical build --clean` after each phase to catch issues early
- Commit after each phase or logical task group with conventional commit messages
